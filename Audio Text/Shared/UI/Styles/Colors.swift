import SwiftUI

extension Color {
    static let recordingRed = Color.red
    static let recordingBlue = Color.blue
    static let backgroundPrimary = Color(.systemBackground)
    static let backgroundSecondary = Color(.secondarySystemBackground)
}
